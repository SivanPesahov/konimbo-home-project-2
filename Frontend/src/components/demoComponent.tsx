function DemoComponent({ name, description, url }: any) {
  return (
    <>
      <div>{name}</div>
      <div>{description}</div>
      <div>{url}</div>
    </>
  );
}

export default DemoComponent;
